# Server
server.port=8080
spring.application.name=job-platform

# Database (override via env in docker-compose)
spring.datasource.url=jdbc:mysql://localhost:3306/job_platform?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Amos1436
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=${APP_JWT_SECRET:change-this-super-secret-jwt-key}
app.jwt.expiration=86400000

# Email
app.mail.enabled=false
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=amostona84@gmail.com
spring.mail.password=clbr pxwq gsho wnod
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File upload
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=20MB
file.upload-dir=uploads

# Frontend URL for email links
app.frontend.url=http://localhost:3000

# Thymeleaf
spring.thymeleaf.cache=false

# OpenAPI
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
